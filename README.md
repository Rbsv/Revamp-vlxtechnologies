# VLx Technologies — HTML/CSS/JS Revamp

Pure static implementation. No React, Next.js, Tailwind, or build step.

## Run
Open `index.html` directly, or upload the folder to your hosting.

## Replace before production
- Replace placeholder portfolio visuals with the real VLx project images/videos.
- Replace `#` social links with official profile URLs.
- Confirm all statistics and client references.
- Add/restore existing SEO landing pages and 301 redirects for any changed URLs.
- Add `sitemap.xml`, `robots.txt`, favicon, and production Open Graph image.
- Connect the contact form to the preferred mail/API endpoint.

## SEO
The page uses semantic headings, descriptive title/description, canonical URL, Open Graph metadata, accessible navigation and real HTML text rather than canvas-based text.
